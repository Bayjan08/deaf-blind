import 'package:flutter/material.dart';

/// §6.2 Original video + synced sign-avatar overlay (PiP or full-screen).
class AvatarVideoPlayerScreen extends StatelessWidget {
  const AvatarVideoPlayerScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Player')));
}
