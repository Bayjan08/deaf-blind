import 'package:flutter/material.dart';

/// §2.3 Duolingo-style level map; houses evolve into a castle.
class LevelMapScreen extends StatelessWidget {
  const LevelMapScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Level Map')));
}
