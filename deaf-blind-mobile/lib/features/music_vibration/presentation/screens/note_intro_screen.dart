import 'package:flutter/material.dart';

/// §3.3 Note intro: background color + vibration pattern, repeated.
class NoteIntroScreen extends StatelessWidget {
  const NoteIntroScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Note')));
}
