import 'package:flutter/material.dart';

/// §6.2 Pick a video to translate.
class UploadScreen extends StatelessWidget {
  const UploadScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Upload video')));
}
