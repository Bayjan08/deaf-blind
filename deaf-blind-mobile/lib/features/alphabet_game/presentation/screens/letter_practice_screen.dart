import 'package:flutter/material.dart';

/// §2.4 Practice: camera (2/3) + target letter; on-device gesture recognition.
class LetterPracticeScreen extends StatelessWidget {
  const LetterPracticeScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Practice')));
}
