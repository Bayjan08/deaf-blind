import 'package:flutter/material.dart';

/// §2.4 Camera view feeding the on-device gesture classifier (core/ml).
class CameraPracticeView extends StatelessWidget {
  const CameraPracticeView({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
