fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
# SQLAlchemy async (3.5+ required for Python 3.14)
greenlet==3.5.2

# Cloud SQL connector (used on Cloud Run instead of raw TCP)
cloud-sql-python-connector[asyncpg]==1.13.0

# Real-time live-class relay (§1)
python-socketio==5.12.1

# Video meetings (LiveKit)
livekit-api==0.8.2
PyJWT==2.10.1

# AI — Vertex AI / Gemini (same approach as cistech)
# Local dev: needs credentials/vertex-service-account.json
# Cloud Run: uses Application Default Credentials automatically (no key needed)
google-cloud-aiplatform==1.71.1

# Slovo RSL recognition (hukenovs/slovo via third_party/slovo)
onnxruntime==1.20.1
opencv-python-headless==4.10.0.84
omegaconf==2.3.0
scikit-learn==1.5.2   # used by build_landmark_index.py for preprocessing

# firebase-admin — add when wiring auth
