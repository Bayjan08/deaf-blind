import 'package:flutter/material.dart';

/// §2.4 End-of-level exam: random subset of letters; pass unlocks next level.
class ExamScreen extends StatelessWidget {
  const ExamScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Exam')));
}
