import 'package:flutter/material.dart';

/// §2.4 Letter intro: image + glyph + looping sign-gesture animation.
class LetterIntroScreen extends StatelessWidget {
  const LetterIntroScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Letter')));
}
