# Copy this to .env and fill in real values. NEVER commit .env.
ENV=dev
PORT=9000

# --- Database (local dev) ---
# Get the public IP from: GCP Console -> Cloud SQL -> deaf-blind-db-dev -> Overview -> Public IP
DATABASE_URL=postgresql+asyncpg://postgres:aSQr0&@)=rAmbH}A@YOUR_CLOUD_SQL_PUBLIC_IP:5432/postgres

# --- Database (Cloud Run) ---
# Leave these empty for local dev — only needed on Cloud Run
CLOUD_SQL_CONNECTION_NAME=deaf-blind-500005:europe-west1:deaf-blind-db-dev
DB_USER=postgres
DB_PASS=aSQr0&@)=rAmbH}A
DB_NAME=deafblind
