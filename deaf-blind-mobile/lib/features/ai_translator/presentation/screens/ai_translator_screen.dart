import 'package:flutter/material.dart';

/// §7.3 Agent UI: mic / camera / text input -> avatar / text / speech output.
class AiTranslatorScreen extends StatelessWidget {
  const AiTranslatorScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('AI Translator')));
}
