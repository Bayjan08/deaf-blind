import 'package:flutter/material.dart';

/// §3.3 Recognition game: feel the pattern, pick the note.
class RecognitionGameScreen extends StatelessWidget {
  const RecognitionGameScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Guess the note')));
}
