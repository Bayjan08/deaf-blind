import 'ai_translator_remote_source.dart';

/// §7 Thin wrapper reusing core/translation + core/ml. Stub.
class AiTranslatorRepository {
  AiTranslatorRepository(this._remote);
  final AiTranslatorRemoteSource _remote;
}
