import 'package:flutter/material.dart';

/// §4.3 Articulation animation + mic attempt + visual-first feedback.
class PronunciationLessonScreen extends StatelessWidget {
  const PronunciationLessonScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Pronunciation')));
}
