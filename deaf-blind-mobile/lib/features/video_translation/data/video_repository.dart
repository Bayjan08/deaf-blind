import 'video_remote_source.dart';

/// §6 Coordinates upload + job polling + core/translation playback. Stub.
class VideoRepository {
  VideoRepository(this._remote);
  final VideoRemoteSource _remote;
}
