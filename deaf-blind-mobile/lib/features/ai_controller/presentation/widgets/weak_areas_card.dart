import 'package:flutter/material.dart';
class WeakAreasCard extends StatelessWidget {
  const WeakAreasCard({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
