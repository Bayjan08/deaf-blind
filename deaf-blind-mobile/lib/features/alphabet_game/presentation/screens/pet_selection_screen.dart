import 'package:flutter/material.dart';

/// §2.2 Onboarding — horizontal carousel of pets to choose from.
class PetSelectionScreen extends StatelessWidget {
  const PetSelectionScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Pick a pet')));
}
