import 'package:flutter/material.dart';

/// §1.5/§1.6 Layout: main video, avatar panel, caption bar, self-view.
class LiveClassScreen extends StatelessWidget {
  const LiveClassScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Live Class')));
}
