/// §2 The student's companion pet (evolves with progress).
class Pet {
  Pet({required this.id, required this.species, required this.level});
  final int id;
  final String species;
  final int level;
}
